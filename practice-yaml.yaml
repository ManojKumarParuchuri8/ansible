- name: packages
  hosts: web 
  become: yes 
  tasks:
  - name: Installing packages
    ansible.builtin.package:
      name: "{{item}}""
      state: present 
    loop:
    - git 
    - ansible
    - nginx 